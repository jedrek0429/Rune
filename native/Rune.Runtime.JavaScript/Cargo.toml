[package]
name = "rune-runtime-javascript-experiment"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
rquickjs = { version = "0.12.2", features = ["parallel"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[lints.rust]
unsafe_op_in_unsafe_fn = "deny"

[lints.clippy]
all = "deny"
pedantic = "deny"
