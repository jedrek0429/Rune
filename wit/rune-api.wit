// <auto-generated by Rune.Api.Generator />
package rune:api@0.1.0;

interface types {
    record user {
        id: u64,
        username: string,
    }

    record message {
        id: u64,
        channel-id: u64,
        content: string,
        author: user,
    }

    record message-delete-event-args {
        channel-id: u64,
        guild-id: option<u64>,
        message-id: u64,
    }

    record message-reaction-emoji {
        animated: bool,
        id: option<u64>,
        name: option<string>,
    }

    enum reaction-type {
        normal,
        burst,
    }

    record message-reaction-add-event-args {
        burst: bool,
        channel-id: u64,
        emoji: message-reaction-emoji,
        guild-id: option<u64>,
        message-author-id: option<u64>,
        message-id: u64,
        %type: reaction-type,
        user-id: u64,
    }

    record message-reaction-remove-event-args {
        burst: bool,
        channel-id: u64,
        emoji: message-reaction-emoji,
        guild-id: option<u64>,
        message-id: u64,
        %type: reaction-type,
        user-id: u64,
    }

}

world message-create-rune {
    use types.{message};
    import reply: func(content: string);
    export handle: func(message: message);
}

world message-delete-rune {
    use types.{message-delete-event-args};
    import reply: func(content: string);
    export handle: func(args: message-delete-event-args);
}

world message-reaction-add-rune {
    use types.{message-reaction-add-event-args};
    import reply: func(content: string);
    export handle: func(args: message-reaction-add-event-args);
}

world message-reaction-remove-rune {
    use types.{message-reaction-remove-event-args};
    import reply: func(content: string);
    export handle: func(args: message-reaction-remove-event-args);
}
